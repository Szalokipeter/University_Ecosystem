<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:UniversityApp.ViewModels"
             x:Class="UniversityApp.Views.PersonalEventsListView"
             Title="Personal Events">

    <ContentPage.BindingContext>
        <viewmodels:PersonalEventsListViewModel/>
    </ContentPage.BindingContext>

    <Grid>
        <CollectionView ItemsSource="{Binding PersonalEvents}">
            <CollectionView.ItemsLayout>
                <GridItemsLayout Orientation="Vertical" 
                                 Span="2"
                                 VerticalItemSpacing="5"
                                 HorizontalItemSpacing="5"/>
            </CollectionView.ItemsLayout>
            
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <StackLayout  Padding="10" BackgroundColor="Navy">
                        <Image Source="{Binding ImageName}" HeightRequest="200"/>
                        <Label Text="{Binding Author}" TextColor="White" FontSize="Small"
                               HorizontalTextAlignment="Center"/>
                        <Label Text="{Binding Title}" TextColor="White" FontSize="Medium"
                               HorizontalTextAlignment="Center"/>
                    </StackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>
</ContentPage>